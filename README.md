## 🛠 Мок-сервер

📖 **[Подробная документация](documentation.md)** — структура проекта, добавление эндпоинтов, сценарии, подключение к фронту.

### Архитектура и принцип работы

Сервер автоматически выстраивает структуру эндпоинтов на основе файловой системы. Процесс инициализации проходит в несколько этапов:

* **Сканирование конфигурации**: При старте сервер анализирует директорию `env`. 
* **Группировка**: Подпапки первого уровня группируют API по портам и базовым путям согласно настройкам в файле `config.ts`.
* **Построение маршрутов**: В каждой группе сканируется папка `routes`.
    * Каждая вложенная папка становится частью **URL-пути** API.
    * Конечным звеном пути является файл, содержащий функции для обработки HTTP-методов (GET, POST и т.д.).
* **Обработка методов**: Названия функций соответствуют методам.
    > 💡 **Примечание**: Для метода `DELETE` используется функция с именем `del`.

---

### 🛠 Технологический стек

Проект реализован как типизированный мок-сервер для имитации работы бэкенда.

### Core
- **TypeScript (v5.9)**: Статическая типизация для повышения надежности кода.
- **Express (v5.2)**: Веб-фреймворк для обработки HTTP-запросов.
- **Faker (v5.5)**: Генерация случайных данных для тестирования интерфейсов.

### Инфраструктура
- **tsc-watch**: Автоматическая пересборка и перезапуск сервера при изменении файлов.
- **rimraf**: Удаление папки `dist` перед новой сборкой для предотвращения конфликтов версий.

## 🏃 Запуск проекта

```bash
# Установка зависимостей
npm install

# Запуск сервера в режиме разработки
npm start
```

### 🎭 Сценарии мок-сервера

Система позволяет переключать ответы сервера (сценарии) с помощью заголовков и локального хранилища.

* **Хранение данных**: `localStorage`.
* **Ожидаемый заголовок**: `x-scenario`.
* **Структура ключа**: `method-<путь-апи>`, где путь — это `pathname`.

#### Пример использования:
Для запроса по адресу `api/v1/module1/list`:
| Параметр | Значение |
| :--- | :--- |
| **Ключ в localStorage** | `GET-api-v1-module1-list` |
| **Значение ключа** | `http400` |
| **Заголовок запроса** | `x-scenario: http400` |
